apiVersion: v1
kind: Service
metadata:
  name: {{ include "postbrain.fullname" . }}
  labels:
    {{- include "postbrain.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "postbrain.selectorLabels" . | nindent 4 }}
