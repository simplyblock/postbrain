# $Id$

PortSystem          1.0
PortGroup           github 1.0

github.setup        simplyblock postbrain 0.0.0 v
revision            0
name                postbrain-client
categories          devel
maintainers         nomaintainer
license             Apache-2
description         Postbrain CLI client
long_description    Postbrain client provides command-line utilities for interacting with a Postbrain server.

checksums           rmd160  REPLACE_WITH_RMD160 \
                    sha256  REPLACE_WITH_SHA256 \
                    size    REPLACE_WITH_SIZE

depends_build       bin:go:go

use_configure       no

build.cmd           make
build.target        build

destroot {
    xinstall -m 0755 ${worksrcpath}/postbrain-cli ${destroot}${prefix}/bin/postbrain-cli
}
