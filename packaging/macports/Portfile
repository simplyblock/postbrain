# $Id$

PortSystem          1.0
PortGroup           github 1.0

github.setup        simplyblock postbrain 0.0.0 v
revision            0
name                postbrain
categories          devel
maintainers         nomaintainer
license             Apache-2
description         Persistent memory and knowledge server for coding agents
long_description    Postbrain provides server and CLI components for shared memory and knowledge across coding agents.

checksums           rmd160  REPLACE_WITH_RMD160 \
                    sha256  REPLACE_WITH_SHA256 \
                    size    REPLACE_WITH_SIZE

depends_build       bin:go:go

use_configure       no

build.cmd           make
build.target        build

destroot {
    xinstall -m 0755 ${worksrcpath}/postbrain ${destroot}${prefix}/bin/postbrain
    xinstall -m 0755 ${worksrcpath}/postbrain-cli ${destroot}${prefix}/bin/postbrain-cli
    xinstall -d ${destroot}${prefix}/etc/postbrain
    xinstall -m 0644 ${worksrcpath}/config.example.yaml ${destroot}${prefix}/etc/postbrain/config.yaml
}
