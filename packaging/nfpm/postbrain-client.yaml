name: postbrain-client
arch: ${ARCH}
platform: linux
version: ${VERSION}
section: default
priority: optional
maintainer: Simplyblock <opensource@simplyblock.io>
description: Postbrain CLI client
vendor: Simplyblock
homepage: https://github.com/simplyblock/postbrain
license: Apache-2.0
contents:
  - src: ./dist/linux-${ARCH}/postbrain-cli
    dst: /usr/bin/postbrain-cli
    file_info:
      mode: 0755
